apiVersion: v1
kind: Pod
metadata:
  name: drop-capabilities
  annotations:
    readableName: Pod without dropped capabilities
    vulnerabilityReason: TODO
spec:
  containers:
    - name: secure
      image: ubuntu
      command: [ "sh", "-c", "sleep 1h" ]
      securityContext:
        readOnlyRootFilesystem: true
        allowPrivilegeEscalation: false
        runAsNonRoot: true
        seccompProfile:
          type: "RuntimeDefault"